{% extends 'base.html.twig' %}

{% block title %}Editar Estrutura{% endblock %}

{% block body %}

    <h1>Editar Estrutura</h1>
    <div class="row">
        <div class="mb-3 col-6">
            {{ include('admin/campaign_structure/_form.html.twig', {'button_label': 'Update'}) }}
            <a href="javascript:history.back()" class="btn btn-secondary float-start ms-3 mt-3 text-white">Voltar</a>


            {{ include('admin/campaign_structure/_delete_form.html.twig') }}
        </div>
        <div class="mb-3 col-6">
            <img class="w-100" id="sample-image" src="{{ path('app_admin_image_temp_from_structure',{'campaignId':campaign_structure.campaign.id}) }}">
{#            {{ include('admin/post/post-img.hml.twig', {'post': campaign_structure.postTexts.0.post}) }}#}
        </div>
    </div>
    <h5>Ajuda</h5>
    <img style="width: 300px" src="{{ asset('assets/images/help-margin.png') }}">








{% endblock %}
