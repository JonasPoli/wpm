<form method="post" action="{{ path('app_admin_campaign_step3_delete', {'id': campaign_structure.id,'campaignId':campaign.id}) }}" onsubmit="return confirm('Quer mesmo apagar este item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ campaign_structure.id) }}">
    <button class="btn btn-danger text-white ms-3 mt-3">Delete</button>
</form>
