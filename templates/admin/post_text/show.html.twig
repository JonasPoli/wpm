{% extends 'base.html.twig' %}

{% block title %}PostText{% endblock %}

{% block body %}
    <h1>PostText</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ post_text.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ post_text.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_post_text_index') }}">back to list</a>

    <a href="{{ path('app_admin_post_text_edit', {'id': post_text.id}) }}">edit</a>

    {{ include('admin/post_text/_delete_form.html.twig') }}
{% endblock %}
