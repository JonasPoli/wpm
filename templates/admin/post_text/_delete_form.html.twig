<form method="post" action="{{ path('app_admin_post_text_delete', {'id': post_text.id}) }}" onsubmit="return confirm('Quer mesmo apagar este item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ post_text.id) }}">
    <button class="btn btn-danger text-white ms-3 mt-3">Delete</button>
</form>
